def RechercheDiviseur(nb):    '''    Fonction qui prend en entrée un entier strictement supérieur à 1 nb    et renvoie son plus petit diviseur autre que 1.    Si elle n'en trouve pas (que le nombre est donc premier), elle    renvoie 0.    '''    div = 0    i = 2    while i < nb:        # On vérifie si nb est un multiple de i        if nb % i == 0:            # Dès qu'on en a trouvé un on le renvoie            div = i            return div        i += 1    # On atteindra ce point si on n'a pas trouvé de diviseur et on renverra donc la valeur initiale de div, soit 0.    return div